# Komga-CN 1.26 executable baseline

Captured on 2026-07-12 from `feature/komga-1.28-modern-ui`.

## Immutable host evidence

- `/root/komga-1.26-routes.txt`: 41 normalized, unique, byte-sorted router paths.
- `/root/komga-1.26-kotlin.sha256`: 550 SHA-256 records for Kotlin sources.
- `/root/komga-1.26-web-services-stores.sha256`: SHA-256 records for `komga-webui/src/services` and `komga-webui/src/store.ts`.

Regenerate route evidence with `scripts/verify-route-parity.sh /root/komga-1.26-routes.txt`. Runtime behavior is checked by `scripts/smoke-komga.sh http://127.0.0.1:25610`.

## Result

Route parity and the published 1.26.0 container smoke checks pass. The source baseline is not fully executable under the requested full build gate: `./gradlew clean test build` fails on pre-existing ktlint violations. The Web build exits zero but its type-check worker exhausts the default Node heap; this must not be interpreted as a clean build.

## Final Task 2 gate (2026-07-13)

The preceding paragraph records the original baseline failure and is retained as historical evidence. After the documented Task 2 remediations, clean commit `042f2d735d063a126597b53e0b28a193667a7650` is finally green:

- UTC JVM / UTC MySQL: exact `./gradlew clean test build --no-daemon` ran 897 tests and finished `BUILD SUCCESSFUL in 3m 11s` with 40 tasks executed (`/root/komga-task2-full-gate-042f2d73-utc.log`).
- Asia/Shanghai JVM / UTC MySQL, using production-shape JDBC URLs without `serverTimezone` or `connectionTimeZone`: exact `./gradlew :komga:test --tests '*LibraryDaoTest' --no-daemon` finished `BUILD SUCCESSFUL in 20s` (`/root/komga-task2-timezone-matrix-local.log`).
- `git grep -nEi 'serverTimezone|connectionTimeZone|user\.timezone|TZ=' -- ':(glob)**/src/main/resources/**' ':(glob)**/src/test/resources/**' ':Dockerfile'` returned no matches; resources do not force a timezone.

Final gate status: **GREEN**.

## Task 2 Vuetify 2.7.1 advisory backports (2026-07-13)

- `GHSA-3jp5-5f8r-q2wg`: `mergeDeep` now iterates own keys only and rejects `__proto__`, `prototype`, and `constructor` through `patch-package`.
- `GHSA-9w3x-85mw-4fwm`: `VDatePickerTitle` now creates a text VNode (using `\u00a0` for the empty title) instead of assigning formatter output to `domProps.innerHTML`.
- `npm ci` automatically applied `patches/vuetify+2.7.1.patch`; the patch contains runtime JavaScript only and no sourcemap changes.
- All 5 unit suites / 37 tests passed. The 8 GiB `npm run build` completed, including TypeScript checking, in 139.6 seconds.
- Official-registry `npm audit --omit=dev` remains a non-zero baseline at 20 vulnerabilities (6 low, 13 moderate, 1 high). Neither backported Vuetify advisory appears as a direct audit finding; Vuetify remains in the dependency path of the separate unfixed Vue 2 ReDoS advisory. No audit suppression was added.

Evidence logs: `/root/komga-task2-vuetify-npm-ci.log`, `/root/komga-task2-vuetify-tests.log`, `/root/komga-task2-vuetify-build.log`, and `/root/komga-task2-vuetify-audit.log`.

## Task 2 Vue 2.7.14 parseHTML ReDoS backport (2026-07-13)

- `GHSA-5j4c-8p2g-v4jx` is backported to Vue's compiler distributions and `vue-template-compiler` with linear index scans; `patch-package` replays both after `npm ci`.
- The bounded RED exceeded its 1,000 ms limit; GREEN completes runtime-compiler and build-time compiler paths within the bound while preserving legal raw-text templates.
- Production webpack stats contains only `vue.runtime.esm.js`, so `parseHTML` is not browser-bundle reachable; the patched build-time SFC path remains in the threat boundary.
- All 6 unit suites / 20 tests passed. The final clean-install 8 GiB production build and type checking completed in 138.528 seconds.
- Official-registry audit remains 20 findings (6 low, 13 moderate, 1 high) and still lists Vue because registry audit cannot detect local source backports. No suppression was added.

Detailed evidence: `docs/superpowers/audits/task2-vue-redos.md`.
